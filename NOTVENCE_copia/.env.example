# Copia este archivo a ".env" y pon una clave larga y aleatoria.
# Se usa para firmar los tokens JWT de la API (app/security.py).
# Si no se define, se usa una clave por defecto insegura (solo para desarrollo).
NOTVENCE_SECRET_KEY=
